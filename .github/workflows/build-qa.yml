name: Build QA Image
on:
  push:
    branches: ["qa"]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Login GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Build & push
        run: |
          SHA7=$(echo "${GITHUB_SHA}" | cut -c1-7)
          IMAGE=ghcr.io/${{ github.repository_owner }}/myapp:qa-$SHA7
          docker build -t $IMAGE .
          docker push $IMAGE
